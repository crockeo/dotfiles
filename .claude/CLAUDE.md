# Claude

## General

- When authoring code, never include comments.

- Use `fd` to look for files (instead of `find`).

- Use `rg` to search through files (instead of `grep`). 

## Python

- When running Python code, assume that you should use `uv run ...` by default.
